{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(formView) }}

        {{ form_widget(formView) }}

        <button type="submit">Create</button>

    {{ form_end(formView) }}

{% endblock %}
